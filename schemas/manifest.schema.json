{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Bike Outliner Extension Manifest",
  "description": "The extension's id and display name are derived from the .bkext folder name. The id is the folder name without the .bkext suffix. The display name is the folder name split on '-' with each word capitalized (e.g., 'heading-levels.bkext' becomes 'Heading Levels').",
  "type": "object",
  "required": [
    "version",
    "api_version",
    "permissions",
    "host_permissions"
  ],
  "properties": {
    "$schema": {
      "type": "string",
      "description": "The schema to verify this document against."
    },
    "description": {
      "type": "string",
      "description": "A brief description of what the extension does."
    },
    "author": {
      "type": "string",
      "description": "The author of the extension."
    },
    "version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "The version of the extension, following semantic versioning (e.g., 1.2.3)."
    },
    "api_version": {
      "type": "string",
      "pattern": "^\\d+\\.\\d+\\.\\d+$",
      "description": "The API version this extension is compatible with."
    },
    "permissions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of permissions the extension requires."
    },
    "host_permissions": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of host-specific permissions (e.g., URLs or domains)."
    },
    "enabled": {
      "type": "boolean",
      "description": "Indicates if the extension is currently enabled."
    },
    "install": {
      "type": "boolean",
      "description": "If true, the extension will be automatically installed when built."
    },
    "built-in": {
      "type": "boolean",
      "description": "If true, the extension is a built-in to Bike Outliner."
    },
    "symbol": {
      "type": "string",
      "description": "Optional SF Symbol name for the extension, used as default for commands."
    },
    "url": {
      "type": "string",
      "format": "uri",
      "description": "URL for the extension's homepage or source code."
    }
  },
  "additionalProperties": false
}
